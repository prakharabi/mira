# Secrets
.env

# Python
venv/
__pycache__/
*.pyc

# Node
node_modules/

# Packaged app (rebuild with: cd electron && node scripts/build_app.js)
electron/dist/

# Third-party (re-fetch via setup steps, not tracked)
whisper.cpp/
daemon/bin/

# Runtime / personal data
daemon/meetings/
daemon/chat_history/
daemon/tts_output/
daemon/mira.log
daemon/mira_error.log
daemon/out.aiff
daemon/settings.json
daemon/personal_ngrams.json
daemon/google_tokens.json
daemon/automations.json
daemon/dumpbox/
daemon/memory_store/
# lead lists hold scraped phone numbers and emails -- personal data
daemon/leads/
# outreach config (mailbox app password), campaigns, message log, do-not-contact list
daemon/outreach/
daemon/telegram_voice/
daemon/wakeword/command_recording.wav
daemon/wakeword/command_recording.converted.wav
daemon/wakeword/command_recording.converted.wav.txt
electron/electron.log

# macOS
.DS_Store

# Stale loose helper builds -- the app runs the copies inside the .app bundles
# (TabTap.app, MicHelper.app, AXHelper.app, AudioHelper.app). Rebuild with the
# swiftc commands in CONTRIBUTING.md.
/tab_tap
/electron/tab_tap
/electron/mic_helper
/electron/mic_test_helper
/electron/ax_helper
